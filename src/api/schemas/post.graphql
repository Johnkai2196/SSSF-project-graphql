scalar DateTime

type Post {
  id: ID!
  text: String!
  user: User!
  image: String!
  dateAdded: DateTime
}

type Query {
  posts: [Post]
}
type Mutation {
  createPost(text: String!, image: String): Post
}
